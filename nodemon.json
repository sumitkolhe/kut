{
  "watch": ["server"],
  "ext": "ts,js,json",
  "ignore": ["node_modules", "coverage", "dist", ".git"],
  "exec": "npm run lint && ts-node -r tsconfig-paths/register ./src/server/server.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
